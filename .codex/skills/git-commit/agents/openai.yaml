interface:
  display_name: "Git Commit"
  short_description: "Create conventional git commits"
  default_prompt: "Use $git-commit to inspect the current diff and create a focused conventional commit."
